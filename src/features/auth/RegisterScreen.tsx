import {StyleSheet, Text, View} from 'react-native';
import React, {FC} from 'react';

const RegisterScreen: FC = () => {
  return (
    <View>
      <Text>RegisterScreen:FC</Text>
    </View>
  );
};

export default RegisterScreen;

const styles = StyleSheet.create({});
